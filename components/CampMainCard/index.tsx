import React, { FC } from 'react';
import { CampInfo } from "@typings/interface";

interface Props {
    data : CampInfo;
}

// const CampMainCard : FC<Props> = ({data}) => {
const CampMainCard = () => {
    return(
        11111111111
    );
}

export default CampMainCard;